!function(window) {

  var cc = function( element ) {

    // 文字列以外はfalseを返す
    if( typeof element !== 'string' ) {

      return false;
    }

    // インスタンスを生成
    return new cc.prototype.init( element );
  }

  calc.prototype = {

    // コンストラクタ
    init: function( element ) {

      this._element = num;
    }
  }
}
